outdir:
    /path/to/out 

logdir:
    /path/to/log

sniffles_se:
    5

survivor:
    distance: 500
    caller_support: 1
    same_type: 1
    same_strand: -1
    estimate_distance: -1
    minimum_size: -1

annotbed:
    /path/to/bed

threads:
    per_sample: 10
    per_chr: 12
    all: 320

genome:
    /path/to/ref

regions:
    /path/to/regions.bed

vcfanno_conf_svs:
    /path/to/vcfanno_bed.conf.svs.38.toml

vcfanno_conf_snvs:
    /path/to/vcfanno_bed.conf.snvs.38.toml

samples:
    SAMPLE1: /path/to/SAMPLE1.fq.fof
    SAMPLE2: /path/to/SAMPLE2.fq.fof
